// Basic Data Type

// String
let firstName: string = "Shohaib";

// Number
let roll: number = 123;

// Boolean
let isAdmin: boolean = true;

// undefined
let x: undefined = undefined;

// null
let y: null = null;

let a: number;

// a = "hello";
a = 123;

// Array

let friends: string[] = ["Mel", "Rose"];
let rollNumber: number[] = [1, 2]

// In TypeScript, a tuple is a data structure that allows you to store a fixed-size, ordered collection of elements of different types. It's similar to an array, but each element can have its own specific type, and the number of elements in a tuple is fixed once it's declared.

let myTuple: [string, number, boolean] = ["Mr. Tiger", 27, true]
